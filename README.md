# Simple Interest Calculator

This project contains a bash script that calculates the simple interest based on the input principal, rate, and time.

## Usage

Run the `simple-interest.sh` file and provide the following arguments:
- Principal (P)
- Rate of Interest (R)
- Time (T)

Example:

```bash
bash simple-interest.sh 1000 5 2
